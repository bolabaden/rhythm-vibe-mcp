"""JSON serialization options for tool results and API responses."""

from __future__ import annotations

# Indent for pretty-printed JSON (tool results, healthcheck, etc.)
JSON_INDENT = 2

# Default empty JSON object string (e.g. for optional payload_json)
JSON_EMPTY_OBJECT = "{}"
